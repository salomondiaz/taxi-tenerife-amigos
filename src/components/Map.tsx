
import Map from './map/Map';
import { MapCoordinates } from './map/types';

// We're exporting the Map component as default
export default Map;

// Also exporting MapCoordinates type for convenience
export type { MapCoordinates };
